{
  "buildCommand": "curl -fsSL https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.19.0-stable.tar.xz | tar xJ && export PATH=\"$PATH:$PWD/flutter/bin\" && flutter config --enable-web && flutter build web --release",
  "outputDirectory": "build/web"
}
